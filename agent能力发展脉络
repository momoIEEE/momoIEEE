可以将agent能力分为能被模型内化和不会内化的部分。

能被模型内化的能力
推理-行动循环、工具调用决策
推理-行动循环是怎么被"内化"的
2022 年 ReAct 的做法，是把 Thought → Action → Observation 写成提示词文本让模型输出，但"用文本提示来承载这个模式"的实现方式实现的这个模式十分脆弱。
相关研究表明：仅仅是同义词替换就能让某个基准上的任务成功率从 27.6% 暴跌到 1.6%。

跃迁 1：从文本提示到 API 契约（2023-2024）
OpenAI 的 Function Calling、Anthropic 的 Tool Use 把"Action"从自由文本改为结构化 JSON 输出，Thought-Action-Observation 这个循环被吸进了 API 协议层。

跃迁 2：从"行动-推理交替"到"推理内嵌行动"（2025）
 DeepSeek V3.2 实现首个"Reasoning-in-Action" 范式：模型在 reasoning_content 里保持完整的推理上下文，自主判断调用时机和参数，把工具输出直接融进推理链。
（本质仍在使用跃迁1建立的协议标准）
这一次跃迁，实际上让模型学会了：
● 识别自己的知识边界，即什么时候该查。
● 选择对的工具
● 把工具结果融回推理链
在这一步之前，跃迁1里实现的
跃迁 3：从"单次调用"到"原生 Agent 工作流"（2026）
（仍在使用跃迁1建立的协议标准，只是加了些字段）
增加的能力：
1.并行工具调用
2.严格结构化输出：即Strict Mode。请求时工具的 JSON Schema 可以标 strict: true，模型保证 arguments 完全符合 schema。这依然是 tool_calls[].function.arguments 这个老字段，只是内容质量被协议层强制保证了。
3.Tool Search：这是协议层比较小的一个扩展，当工具特别多时，你不用把所有 tool 定义都塞进 tools 数组，而是传一个轻量列表 + 声明 tool_search 能力，模型需要时自己查。这是对 tools 字段的使用方式的优化，不是新字段替代旧字段。
4.reasoning_effort 参数:控制模型"想多久"的旋钮，请求参数层的新增。不影响 tool_calls 的返回结构。

不会被内化的能力
协议、治理、状态管理、多agent协作、FinOps
对应ai agent领域里正在结构性增长的几个部分：
● 协议层（MCP、A2A等连接模型与外部系统的标准）
● 记忆/状态管理层（Databricks MemEx 这类可编程暂存区）
● 工作流编排层（多 Agent 协作、GraphPlanner 这类）
● 垂直场景的执行系统（能真跑通退款、能真合并 PR、能真跑数据分析）
